---
title: "Debian Network Install на Dell Inspiron 6400"
date: 2007-09-05
tags: ["life"]
lang: ua
category: life
---

Сьогодні вирішив переставити Лінуху на ноуті, а то в попередній спробі я одразу прописав нестабільний реліз як джерело пакетів і лише згодом зрозумів наскільки він нестабільний… Тому зараз не повторюючи помилок минулого зупинюся на звичайному “testing” 🙂

Для наступних кроків вам знадобляьбся права root і якщо ви при встановленні вибрали не створювати такого користувача, то щоб не набирати “sudo” перед кожною командою краще запустити shell під sudo. Наприклад так:

```
$ sudo bash
```

Крім того, після встановлення дратує, що в консолі воно постійно і дуже голосно пищить. Тому бажано одразу б відрубити PC Speaker:

```
# rmmod pcspkr
```

і зробити так, щоб цей модуль надалі не завантажувався:

```
# echo "blacklist pcspkr" >> /etc/modprobe.d/blacklist
```

Перша справа після встановлення – нормально прописати список пакетів, бо інсталлер якогось дива не пише в sources.list те, що йому кажеш при інсталяції. Не забуваємо, що у нас після встановлення Debian Network Install нема таких чудес цивілізації як Midnight Commander та vim. Мережу в мене нормально розпізнало, тому _/etc/network/interfaces_ мені чіпати не довелось. Але на всяк випадок, по-перше, зверну увагу на нумерацію інтерфейсів:

*   **eth0** – FireWire інтерфейс
*   **eth1** – Wi-Fi картка (для неї потім потрібно буде поставити дрова, бо з коробки не працює)
*   **eth2** – Ethernet картка (власне, те, що нам і потрібно)

А по-друге, приведу налаштування свого _/etc/network/interfaces_ з розрахунку на DHCP:

```
# The loopbacknetwork interface
auto lo
iface lo inet loopback
# The primary network interface
allow-hotplug eth2
iface eth2 inet dhcp
```

Йдемо, значить в _/etc/apt_ правити _sources.list_:

```
# cd /etc/apt # nano sources.list
```

Там додаємо наступні рядки:

```
deb http://debian.org.ua/debian/ testing main contrib non-free
deb-src http://debian.org.ua/debian/ testing main contrib non-free
```

Як варіант можна використовувати _[debain.osdn.org.ua](http://debain.osdn.org.ua)_ – він теж в UA-IX.

А рядок з “deb cdrom…” можна закоментувати поставивши перед ним решітку “#”, щоб при оновленні не дьоргав оптичний привід.

Далі завантажуємо свіжий список пакетів і оновлюємо систему:

```
# apt-get update
# apt-get upgrade
```

Ну а далі в залежності від власних смаків продовжуємо додавати пакети… 😉

### По мірі подальшого налаштування стаття буде оновлюватись…

Наразі паюся з WiFi. Що з того вийде згодом опишу.

### Використана література 🙂

*   [Debian 4.0 Etch on a Dell 6400 (E1505)](http://www.mova.org/~lowry/dell6400-e1505.html)
*   [Dell Inspiron E1505/6400 with Linux (Debian Etch)](http://www.cs.hbg.psu.edu/~txn131/web/Computer/Linux/e1505/dell_e1505.html)
